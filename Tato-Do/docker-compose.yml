#services:
  strapi:
    container_name: strapi
    build: .
    image: strapi:latest
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_CLIENT: mysql
      DATABASE_HOST: TatoDoDB
      DATABASE_PORT: 3306
      DATABASE_NAME: tatodo
      DATABASE_USERNAME: strapi
      DATABASE_PASSWORD: strapi
      JWT_SECRET: 5K8vPxqZ9mYjL3nR7tWfX2cB4dE6gH8k
      ADMIN_JWT_SECRET: 9R3mK5pL7qX2vB4nZ6tW8cY1dF3hJ5g
      APP_KEYS: K8m2vL5pR9qX3nB7tW1cZ4dY6fH9jG2k,R5pL9qX2vB4nZ7tW1cY3dF6hJ8gK1m5p,X3nB7tW1cZ5dY9fH2jG6kM8p2vL4qR7x,B4nZ8tW2cY6dF1hJ4gK7m9p3vL5qR8x1
      NODE_ENV: development
    volumes:
      #- ./config:/opt/app/config
      #- ./src:/opt/app/src
      #- ./package.json:/opt/package.json
      #- ./yarn.lock:/opt/yarn.lock
      #- ./.env:/opt/app/.env
      #- ./public/uploads:/opt/app/public/uploads
      - strapi-src:/opt/app/src              # ✅ Persiste les content-types
      - strapi-uploads:/opt/app/public/uploads
    ports:
      - "1337:1337"
    networks:
      - strapi
    depends_on:
      - TatoDoDB

  TatoDoDB:
    container_name: strapiDB
    platform: linux/amd64 #for platform error on Apple M1 chips
    restart: unless-stopped
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_USER: strapi
      MYSQL_ROOT_PASSWORD: 040505
      MYSQL_PASSWORD: strapi
      MYSQL_DATABASE: tatodo
    volumes:
      - strapi-data:/var/lib/mysql
      #- ./data:/var/lib/mysql # if you want to use a bind folder
    ports:
      - "3307:3306"
    networks:
      - strapi

volumes:
  strapi-data:

networks:
  strapi:
    name: strapi
    driver: bridge
